# =============================================================
# Perfil DEV — PostgreSQL local
# =============================================================
spring.datasource.url=jdbc:postgresql://localhost:5432/mylifedb
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=mylife
spring.datasource.password=mylife123

# Dialeto detectado automaticamente pelo Hibernate para PostgreSQL
